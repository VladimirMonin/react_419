# Знакомство с React и сборщиками проектов ⚛️

Привет! Добро пожаловать на наш курс по React. Сегодня мы начинаем увлекательное путешествие в мир современной веб-разработки. React — это не просто популярный инструмент, это мощная библиотека от Facebook (ныне Meta), которая изменила подход к созданию пользовательских интерфейсов. Впервые она была внедрена в новостную ленту Facebook еще в 2011 году и с тех пор стала одним из самых востребованных навыков для фронтенд-разработчика.

Мы будем изучать самую актуальную на 2025 год версию — **React 19**. Но не стоит беспокоиться, если вы наткнулись на курсы по предыдущим версиям. Основы React, особенно после появления хуков (hooks), остаются стабильными. Главное — понять ключевые концепции, и вы сможете работать с любой версией.

>[!info]
>
>#### Что такое хуки? 🤔
>
>Хуки (hooks) — это функции, которые позволяют вам «подключаться» к состоянию и жизненному циклу React из функциональных компонентов. Если в курсе, который вы нашли, упоминаются `useState` и `useEffect` — смело изучайте, это актуально!

## Подготовка 🛠️

Прежде чем мы погрузимся в код, нужно подготовить наше рабочее окружение. Это как подготовить холст и краски перед написанием картины. Нам понадобится всего один основной инструмент — `Node.js`.

`Node.js` — это среда выполнения JavaScript, которая позволяет запускать JS-код вне браузера. Но что еще важнее для нас — вместе с `Node.js` устанавливается `npm` (Node Package Manager), менеджер пакетов, который открывает доступ к огромной экосистеме готовых решений и инструментов, включая сам React и его сборщики.

После установки `Node.js` (просто скачайте LTS-версию с официального сайта и следуйте инструкциям), мы готовы создать наш первый React-проект. Мы будем использовать `Vite` — современный и невероятно быстрый сборщик проектов.

Откройте ваш терминал (командную строку) и выполните следующую команду:

```bash
npm create vite@latest . -- --template react
```

Эта команда запустит процесс создания нового проекта. `my-react-app` — это название папки вашего будущего приложения. Вы можете выбрать любое другое имя.

>[!highlight]
>
>#### Создание проекта в текущей папке 📂
>
>Если вы уже создали папку для своего проекта и находитесь в ней в терминале, вы можете использовать точку (`.`) вместо `my-react-app`, чтобы `Vite` развернул проект прямо здесь: `npm create vite@latest . -- --template react`

После того как `Vite` создаст структуру проекта, терминал подскажет вам следующие шаги. Нам нужно перейти в папку проекта и установить все зависимости, которые описаны в файле `package.json`.

```bash
cd my-react-app
npm install```

Команда `npm install` скачает все необходимые библиотеки (React, ReactDOM и другие) в папку `node_modules`. Теперь все готово к запуску! Чтобы увидеть наше приложение в действии, выполните:

```bash
npm run dev
```

Эта команда запустит сервер для разработки, и в вашем терминале появится ссылка (обычно `http://localhost:5173`), которую можно открыть в браузере. Поздравляю, вы только что создали и запустили свое первое React-приложение!

## Сравнение популярных сборщиков 📊

Когда мы говорим о создании современных веб-приложений, мы не можем обойти стороной тему **сборщиков** (bundlers). Что это такое? Представьте, что ваше приложение — это сложный пазл, состоящий из множества кусочков: JS-файлов, CSS-стилей, изображений. Сборщик — это инструмент, который берет все эти кусочки и аккуратно собирает их в единое, оптимизированное целое, понятное для браузера.

В мире React существует несколько популярных сборщиков, и выбор правильного инструмента может значительно ускорить вашу разработку.

>[!info]
>
>#### Ключевые игроки на рынке сборщиков
>
>- **Vite**: Наш выбор для этого курса. `Vite` (произносится «вит», с французского — «быстро») завоевал огромную популярность благодаря своей невероятной скорости и простоте настройки. Он использует современные возможности браузеров, чтобы обеспечить почти мгновенную перезагрузку при изменениях в коде.
>- **Bun**: `Bun` — это не просто сборщик, а целая экосистема инструментов (включая собственный менеджер пакетов и среду выполнения), которая обещает революционную производительность. Это очень перспективный новичок, за которым стоит следить.
>- **Webpack**: `Webpack` — это «ветеран» мира сборщиков. Долгое время он был стандартом де-факто для большинства React-проектов. Он невероятно мощный и гибкий, но его настройка может быть довольно сложной для новичков. Сегодня он все еще используется во многих крупных проектах и фреймворках.

Для старта в 2025 году `Vite` является идеальным выбором, сочетая в себе производительность, простоту и мощные возможности «из коробки».

## Команды работы с `Vite` 🚀

`Vite` предоставляет нам несколько простых команд для управления жизненным циклом нашего приложения. Все они запускаются через `npm run`, что является стандартным способом выполнения скриптов, определенных в файле `package.json`.

Давайте разберемся с основными командами:

```bash
npm run dev
```

Эту команду мы уже использовали. Она запускает **сервер для разработки**. `Vite` будет следить за изменениями в ваших файлах и мгновенно обновлять приложение в браузере, что делает процесс разработки очень комфортным и быстрым.

```bash
npm run build
```

Когда ваше приложение готово к тому, чтобы его увидели пользователи, эта команда **собирает проект**. `Vite` оптимизирует весь ваш код, сжимает файлы и подготавливает его для загрузки на хостинг. Результат сборки помещается в папку `dist`.

```bash
npm run preview
```

После того как вы собрали проект командой `build`, вы можете захотеть **проверить, как он будет работать** в «боевых» условиях, но еще до загрузки на сервер. Эта команда запускает локальный сервер, который показывает содержимое папки `dist`. Это отличный способ финальной проверки перед публикацией.

## Запуск и просмотр базового проекта ✨

Итак, мы запустили наш проект и увидели в браузере приветственную страницу от `Vite` с вращающимися логотипами. Красиво! Но для нашего собственного приложения весь этот демонстрационный код не нужен. Наша цель — получить **чистый холст** для творчества. Давайте проведем небольшую уборку.

### Что можно смело удалять?

- `src/App.css`: Стили для демо-компонента. У нас будут свои.
- `src/assets/react.svg`: Логотип React.
- `public/vite.svg`: Логотип Vite.

### Что нужно отредактировать?

1. **`src/index.css`**: Этот файл содержит глобальные стили. Пока что можно удалить из него все, кроме базовых стилей для сброса отступов, если они вам нужны. Или же просто оставьте его пустым.

2. **`index.html`**: Главный HTML-файл. Уберем иконку `Vite` и поменяем заголовок.

```html
<!doctype html>
<html lang="en">
    <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale-1.0" />
    <title>Мое первое React-приложение</title>
    </head>
    <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
    </body>
</html>
```

3. **`src/App.tsx`**: Сердце нашего демо-приложения. Заменим его содержимое на минимально рабочий компонент.

```tsx
function App() {
    return (
    <div>
        <h1>Привет, мир React!</h1>
    </div>
    );
}

export default App;
```

4. **`src/main.tsx`**: Точка входа, где наше приложение "монтируется" в HTML. Нужно убрать импорт удаленного `index.css` (если вы его удалили) и `App.css`.

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
// import './index.css' // Удаляем или комментируем, если файл пуст

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
    <App />
    </React.StrictMode>,
)
```

>[!warning]
>
>#### Не удаляйте эти файлы! 🛑
>
>Некоторые файлы являются ключевыми для работы проекта, и их удаление может все сломать. К ним относятся:
>
>- `.gitignore`: Указывает Git, какие файлы игнорировать.
>- `package.json` и `package-lock.json`: Управляют зависимостями проекта.
>- `tsconfig.*.json`: Конфигурация TypeScript.
>- `vite.config.ts`: Конфигурация сборщика `Vite`.

После всех этих манипуляций ваше приложение будет отображать только заголовок «Привет, мир React!». Теперь у вас есть идеальная отправная точка для создания чего-то великого!
